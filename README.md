# Elvar releases

Built releases of Elvar, the bookkeeping desk for dkBúbót. `Elvar.exe` (the launcher) fetches the newest `elvar-win64.zip` here on every start.

The source lives in a private repository. Nothing here contains keys or personal data.
